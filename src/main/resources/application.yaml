server:
  port: 8080

spring:
  application:
    name: g34-comment-service

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: b1t
    password: 1596psql
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


